export default function Game({ gameId }) {
    return (
        <div className="p-4">
            <h1 className="text-xl font-bold">Game ID: {gameId}</h1>
            {/* Add real game UI here */}
        </div>
    );
}
